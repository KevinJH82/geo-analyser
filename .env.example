# geo-analyser 环境变量模板
# 复制本文件为 .env 并填入真实值;.env 已被 .gitignore 忽略,不会入库。
# app.py 启动时会自动加载(通过 python-dotenv)。

# ── 矿床类型 LLM 自动识别(DeepSeek,OpenAI 兼容)──
# 未设置则"带入 ROI 自动识别矿床类型"功能自动跳过,前端退回手动两级下拉。
DEEPSEEK_API_KEY=
# 可选:模型名与接口地址(留空用默认)
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com
